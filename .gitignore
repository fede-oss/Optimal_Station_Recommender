#ignore the datasets for the git push
data/
cache/

# Large geopackage files (tracked by Git LFS)
# Note: *.gpkg files are tracked by Git LFS via .gitattributes
# results/**/*.gpkg

# Git pack files
.git/objects/pack/*.pack